
using Xunit;

namespace ExtensionMethodsTests
{
	public class HttpClientTest
	{
		public HttpClientTest()
		{
			//var p = o => {  }
			//var p = new Func<IDictionary<string, object>, Task>(x => async { return; })
			//Microsoft.Owin.Host.HttpListener.OwinHttpListener owin = Microsoft.Owin.Host.HttpListener.OwinServerFactory.Create(,);
			//owin.
		}

		[Fact]
		public void GetGetAsync()
		{

		}
	}
}
